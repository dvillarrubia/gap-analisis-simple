# Configuración de LLM para naming de clusters
# Copia este archivo a .env y configura tus preferencias

# Proveedor de LLM: "ollama", "openai", o "none" (usar sistema regex)
LLM_PROVIDER=none

# Configuración Ollama (local, gratis)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# Configuración OpenAI (requiere API key)
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Temperatura para generación (0.0 = determinístico, 1.0 = creativo)
LLM_TEMPERATURE=0.3

# Máximo de páginas a analizar por cluster para generar nombre
MAX_PAGES_FOR_NAMING=10
